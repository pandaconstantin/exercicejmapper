package com.example.exericejmapper.repository;

import com.example.exericejmapper.StructureComptable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StructureComptableRepository extends JpaRepository<StructureComptable, Long> {
}